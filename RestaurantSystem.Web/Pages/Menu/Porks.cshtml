@page
@model RestaurantSystem.Web.Pages.Menu.PorksModel
@{
    ViewData["Title"] = "Porks";
}
<div class="container-fluid">
    <partial name="MenuBarPartial" />

    <h2>Porks</h2>
    <div class="table-responsive cancelAndCreate">
        <table class="table table-striped">
            <thead>
                <tr class="text-center">
                    <th>Name</th>
                    <th>Price</th>
                    <th>Description</th>
                    <th>DishWeight</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var pork in this.Model.Porks)
                {
                    <tr class="text-center">
                        <td>@pork.Name</td>
                        <td>@pork.Price</td>
                        <td>@pork.Description</td>
                        <td>@pork.DishWeight</td>
                        @if (User.IsInRole("Administrator"))
                        {
                            <td>
                                <a class="btn btn-danger" aria-label="Close" asp-page-handler="BanFood" asp-page="/Menu/Order" asp-route-id="@pork.Id">
                                    <span aria-hidden="true">&times;</span>
                                </a>
                            </td>
                        }
                        else
                        {
                            <td>
                                <a class="btn btn-success" asp-page-handler="OrderFood" asp-page="/Menu/Order" asp-route-id="@pork.Id">Order</a>
                            </td>

                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>