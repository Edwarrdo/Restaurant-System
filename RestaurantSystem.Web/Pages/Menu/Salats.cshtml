@page
@model RestaurantSystem.Web.Pages.Menu.SalatsModel
@{
    ViewData["Title"] = "Salats";
}
<div class="container-fluid">
    <partial name="MenuBarPartial" />

    <h2 class="mb-2">Salads</h2>
    <div class="table-responsive cancelAndCreate">
        <table class="table table-striped">
            <thead>
                <tr class="text-center">
                    <th>Name</th>
                    <th>Price</th>
                    <th>Description</th>
                    <th>Dish Weight</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var salat in this.Model.Salats)
                {
                    <tr class="text-center">
                        <td>@salat.Name</td>
                        <td>@salat.Price</td>
                        <td>@salat.Description</td>
                        <td>@salat.DishWeight</td>
                        @if (User.IsInRole("Administrator"))
                        {
                            <td>
                                <a class="btn btn-danger" aria-label="Close" asp-page-handler="BanFood" asp-page="/Menu/Order" asp-route-id="@salat.Id">
                                    <span aria-hidden="true">&times;</span>
                                </a>
                            </td>
                        }
                        else
                        {
                            <td>
                                <a class="btn btn-success" asp-page-handler="OrderFood" asp-page="/Menu/Order" asp-route-id="@salat.Id">Order</a>
                            </td>

                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>